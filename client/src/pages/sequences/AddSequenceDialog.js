import React, {useState } from "react"
import {AppBar, Toolbar, Typography, IconButton, Slide} from "@material-ui/core"

function Transition(props){
    return (
     <Slide direction="up" {...props} />
    )
}

function AddSequenceDialog(props){
    return (

    )
}